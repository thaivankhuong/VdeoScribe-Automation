---
phase: 15-parity-witness-and-regression-validation
plan: 02
title: Add deterministic parity regression gates for package hashes and env-gated playable media
type: execute
wave: 2
depends_on: [15-01]
requirements: [VAL-01]
files_modified:
  - artifacts/source-parity-demo/check/phase15-regression-baseline.json
  - tests/Whiteboard.Cli.Tests/ParityWitnessRegressionTests.cs
  - tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
  - tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj
  - .planning/phases/15-parity-witness-and-regression-validation/15-02-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - Repeated authored parity runs fail first on manifest drift, frame-artifact drift, or anchor-frame hash drift before any subjective review step.
    - The repo carries an explicit regression baseline for the committed authored witness package instead of relying on ad-hoc manual comparisons.
    - Playable-media validation stays deterministic in automated tests and only performs real encoder smoke when environment variables are configured.
  artifacts:
    - artifacts/source-parity-demo/check/phase15-regression-baseline.json
    - tests/Whiteboard.Cli.Tests/ParityWitnessRegressionTests.cs
    - tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
  key_links:
    - artifacts/source-parity-demo/check/phase15-regression-baseline.json -> tests/Whiteboard.Cli.Tests/ParityWitnessRegressionTests.cs via committed manifest and anchor-hash expectations
    - tests/Whiteboard.Cli.Tests/ParityWitnessRegressionTests.cs -> artifacts/source-parity-demo/out/phase15-review-witness/frame-manifest.json via baseline-package comparison
    - tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs -> artifacts/source-parity-demo/project-engine.json via parity-spec repeated-run and fake-runner playable-media checks
---

## Objective
Turn the authored parity witness into a deterministic regression gate that catches package and media drift with committed baselines and rerunnable tests.

Purpose: Close `VAL-01` by making parity regression review fail on package or media evidence drift, not on informal visual recollection.
Output: A committed regression-baseline manifest, parity-specific deterministic regression tests, and optional real-FFmpeg smoke verification recorded in summary output.

## Context
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-parity-witness-and-regression-validation/15-CONTEXT.md
@artifacts/source-parity-demo/check/phase15-review-bundle.json
@artifacts/source-parity-demo/out/phase15-review-witness/frame-manifest.json
@artifacts/source-parity-demo/out/phase14-fidelity-witness/frame-manifest.json
@artifacts/source-parity-demo/out/phase13-motion-witness/frame-manifest.json
@tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
@tests/Whiteboard.Cli.Tests/PlayableMediaEncodingContractTests.cs
@src/Whiteboard.Export/Services/ExportPipeline.cs

## Tasks
<task type="auto">
  <name>Capture a committed parity regression baseline from the authored witness package</name>
  <files>
artifacts/source-parity-demo/check/phase15-regression-baseline.json
  </files>
  <read_first>
artifacts/source-parity-demo/check/phase15-review-bundle.json
artifacts/source-parity-demo/out/phase15-review-witness/frame-manifest.json
artifacts/source-parity-demo/out/phase14-fidelity-witness/frame-manifest.json
artifacts/source-parity-demo/out/phase13-motion-witness/frame-manifest.json
artifacts/source-parity-demo/check/phase14-review-targets.json
  </read_first>
  <action>Create `artifacts/source-parity-demo/check/phase15-regression-baseline.json` with exact top-level fields `sourceSpec`, `comparisonWitness`, `expectedProjectId`, `expectedFrameCount`, `expectedAudioCueCount`, `expectedTotalDurationSeconds`, `anchorArtifactDeterministicKeys`, `lineage`, and `playableMedia`. Set `comparisonWitness` to `out/phase15-review-witness/frame-manifest.json`, set `expectedProjectId` to `source-parity-authored-witness`, set `expectedFrameCount` to `264`, set `expectedAudioCueCount` to `0`, and set `expectedTotalDurationSeconds` to `11`. Populate `anchorArtifactDeterministicKeys` with the committed `artifactDeterministicKey` values for frames `27`, `72`, `93`, `130`, `185`, and `214`, and record lineage entries for the Phase 13 motion witness, the Phase 14 fidelity witness, and the Phase 15 review witness. Include a `playableMedia` object describing the expected deterministic-test status and the env-gated real-smoke behavior.</action>
  <acceptance_criteria>
1. `artifacts/source-parity-demo/check/phase15-regression-baseline.json` exists and contains `"expectedProjectId": "source-parity-authored-witness"`.
2. `artifacts/source-parity-demo/check/phase15-regression-baseline.json` contains `"expectedFrameCount": 264`.
3. `artifacts/source-parity-demo/check/phase15-regression-baseline.json` contains `"expectedAudioCueCount": 0`.
4. `artifacts/source-parity-demo/check/phase15-regression-baseline.json` contains `"expectedTotalDurationSeconds": 11`.
5. `artifacts/source-parity-demo/check/phase15-regression-baseline.json` contains six `anchorArtifactDeterministicKeys` entries for `27`, `72`, `93`, `130`, `185`, and `214`.
6. `artifacts/source-parity-demo/check/phase15-regression-baseline.json` contains `"comparisonWitness": "out/phase15-review-witness/frame-manifest.json"`.
  </acceptance_criteria>
  <verify>
    <automated>$baseline = Get-Content 'artifacts/source-parity-demo/check/phase15-regression-baseline.json' -Raw | ConvertFrom-Json; if ($baseline.expectedProjectId -ne 'source-parity-authored-witness') { throw "Unexpected project id: $($baseline.expectedProjectId)" }; if ($baseline.expectedFrameCount -ne 264) { throw "Unexpected frame count: $($baseline.expectedFrameCount)" }; if ($baseline.expectedAudioCueCount -ne 0) { throw "Unexpected audio cue count: $($baseline.expectedAudioCueCount)" }; if ($baseline.expectedTotalDurationSeconds -ne 11) { throw "Unexpected duration: $($baseline.expectedTotalDurationSeconds)" }; @(27,72,93,130,185,214) | ForEach-Object { if (-not ($baseline.anchorArtifactDeterministicKeys.frameIndex -contains $_)) { throw "Missing anchor key for frame $_" } }; if ($baseline.comparisonWitness -ne 'out/phase15-review-witness/frame-manifest.json') { throw "Unexpected comparison witness: $($baseline.comparisonWitness)" }</automated>
  </verify>
  <done>The repo has a committed parity regression baseline that names the exact witness package and anchor-frame hashes future runs must preserve.</done>
</task>

<task type="auto">
  <name>Add parity-specific deterministic regression tests and record the media-validation gate</name>
  <files>
tests/Whiteboard.Cli.Tests/ParityWitnessRegressionTests.cs
tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj
.planning/phases/15-parity-witness-and-regression-validation/15-02-SUMMARY.md
  </files>
  <read_first>
tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
tests/Whiteboard.Cli.Tests/PlayableMediaEncodingContractTests.cs
tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj
artifacts/source-parity-demo/check/phase15-regression-baseline.json
artifacts/source-parity-demo/check/phase15-review-bundle.json
artifacts/source-parity-demo/out/phase15-review-witness/frame-manifest.json
src/Whiteboard.Export/Services/ExportPipeline.cs
  </read_first>
  <action>Create `tests/Whiteboard.Cli.Tests/ParityWitnessRegressionTests.cs`, add it to `tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj`, and cover three exact behaviors on `artifacts/source-parity-demo/project-engine.json`: first, two export-package runs with `WHITEBOARD_ENABLE_PLAYABLE_MEDIA=0` must produce byte-identical manifests and byte-identical frame artifacts; second, the current render must match `artifacts/source-parity-demo/check/phase15-regression-baseline.json` for project id, frame count, duration, and the six anchor-frame `artifactDeterministicKey` values; third, a parity-spec run using `ExportPipeline(new PlayableMediaEncoder(new DeterministicWitnessProcessRunner()))` must produce deterministic playable-media metadata across repeated runs when media encoding is enabled with a fake executable path. Reuse the existing `DeterministicWitnessProcessRunner` pattern in `PipelineOrchestratorIntegrationTests.cs` if needed rather than inventing a new media harness. After automated tests pass, run an env-gated real FFmpeg smoke only when both `WHITEBOARD_ENABLE_PLAYABLE_MEDIA=1` and `WHITEBOARD_FFMPEG_PATH` are configured, and record in `15-02-SUMMARY.md` whether the smoke produced an inspectable media artifact or was skipped because the environment was not configured.</action>
  <acceptance_criteria>
1. `tests/Whiteboard.Cli.Tests/ParityWitnessRegressionTests.cs` exists and reads `artifacts/source-parity-demo/check/phase15-regression-baseline.json`.
2. `tests/Whiteboard.Cli.Tests/ParityWitnessRegressionTests.cs` asserts repeated-run manifest equality and repeated-run frame-file byte equality.
3. `tests/Whiteboard.Cli.Tests/ParityWitnessRegressionTests.cs` asserts anchor-frame deterministic keys for `27`, `72`, `93`, `130`, `185`, and `214`.
4. `tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj` includes `ParityWitnessRegressionTests.cs`.
5. `dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~ParityWitnessRegressionTests"` passes.
6. `dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~PipelineOrchestrator_WithPhase12AuthoredWitnessSpec_ProducesEquivalentArtifactsAcrossRepeatedRuns"` still passes.
7. `.planning/phases/15-parity-witness-and-regression-validation/15-02-SUMMARY.md` states either a produced playable-media path or an explicit environment-gated skip.
  </acceptance_criteria>
  <verify>
    <automated>$env:WHITEBOARD_ENABLE_PLAYABLE_MEDIA = '0'; dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~ParityWitnessRegressionTests"; dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~PipelineOrchestrator_WithPhase12AuthoredWitnessSpec_ProducesEquivalentArtifactsAcrossRepeatedRuns"; if ($env:WHITEBOARD_ENABLE_PLAYABLE_MEDIA -eq '1' -and $env:WHITEBOARD_FFMPEG_PATH) { dotnet 'src/Whiteboard.Cli/bin/Debug/net8.0/Whiteboard.Cli.dll' --spec 'artifacts/source-parity-demo/project-engine.json' --output 'artifacts/source-parity-demo/out/phase15-review-witness.mp4'; if (-not (Test-Path 'artifacts/source-parity-demo/out/phase15-review-witness.mp4')) { throw 'Missing real playable-media witness.' } }; $summary = Get-Content '.planning/phases/15-parity-witness-and-regression-validation/15-02-SUMMARY.md' -Raw; if (($summary -notmatch 'playable media path') -and ($summary -notmatch 'environment was not configured')) { throw 'Summary missing media validation outcome.' }</automated>
  </verify>
  <done>Phase 15 closes with parity-specific regression tests that enforce package stability and deterministic media evidence, while keeping real FFmpeg smoke optional and explicit.</done>
</task>

## Verification
```powershell
$env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'
dotnet build 'whiteboard-engine.sln' --no-restore -v minimal /m:1
$env:WHITEBOARD_ENABLE_PLAYABLE_MEDIA = '0'
dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~ParityWitnessReviewBundleTests|FullyQualifiedName~ParityWitnessRegressionTests"
dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~PipelineOrchestrator_WithPhase12AuthoredWitnessSpec_ProducesEquivalentArtifactsAcrossRepeatedRuns"
if ($env:WHITEBOARD_ENABLE_PLAYABLE_MEDIA -eq '1' -and $env:WHITEBOARD_FFMPEG_PATH) { dotnet 'src/Whiteboard.Cli/bin/Debug/net8.0/Whiteboard.Cli.dll' --spec 'artifacts/source-parity-demo/project-engine.json' --output 'artifacts/source-parity-demo/out/phase15-review-witness.mp4' }
```

## Success Criteria
- `VAL-01` is satisfied through committed package baselines and parity-specific repeated-run regression checks.
- Anchor-frame drift is caught through deterministic hash assertions before a reviewer needs to eyeball frame differences.
- Playable-media validation stays two-layered: deterministic fake-runner automation first, optional real-encoder smoke second.

## Output
After completion, create `.planning/phases/15-parity-witness-and-regression-validation/15-02-SUMMARY.md`.
