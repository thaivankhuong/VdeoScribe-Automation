---
phase: 15-parity-witness-and-regression-validation
plan: 01
title: Produce a review-friendly parity witness bundle on top of the authored export package
type: execute
wave: 1
depends_on: []
requirements: [AST-02]
files_modified:
  - artifacts/source-parity-demo/export-phase15-review-witness.ps1
  - artifacts/source-parity-demo/check/phase15-review-bundle.json
  - artifacts/source-parity-demo/out/phase15-review-witness/*
  - tests/Whiteboard.Cli.Tests/ParityWitnessReviewBundleTests.cs
  - tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj
  - .planning/phases/15-parity-witness-and-regression-validation/15-01-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - The canonical parity witness remains a repo-stored export package under `artifacts/source-parity-demo/out/`, not a new runtime mode or screenshot-only review flow.
    - Reviewers can open one stable directory tree and immediately find `frame-manifest.json`, the six anchor-frame SVG artifacts, and the current playable-media status.
    - The Phase 14 authored witness lineage stays explicit so milestone closeout can explain what changed without rediscovering baseline artifacts.
  artifacts:
    - artifacts/source-parity-demo/export-phase15-review-witness.ps1
    - artifacts/source-parity-demo/check/phase15-review-bundle.json
    - artifacts/source-parity-demo/out/phase15-review-witness/*
    - tests/Whiteboard.Cli.Tests/ParityWitnessReviewBundleTests.cs
  key_links:
    - artifacts/source-parity-demo/export-phase15-review-witness.ps1 -> artifacts/source-parity-demo/out/phase15-review-witness/* via the existing CLI `--spec` and `--output` flow
    - artifacts/source-parity-demo/check/phase15-review-bundle.json -> tests/Whiteboard.Cli.Tests/ParityWitnessReviewBundleTests.cs via bundle-driven anchor-frame and output-path assertions
    - artifacts/source-parity-demo/check/phase15-review-bundle.json -> artifacts/source-parity-demo/out/phase14-fidelity-witness/frame-manifest.json via explicit witness lineage metadata
---

## Objective
Package the authored parity route into one review-friendly witness bundle that stays rooted in the existing export-package flow and is easy to inspect during iteration.

Purpose: Close `AST-02` by making the Phase 14 witness output easier to consume without introducing a new parity-specific runtime surface.
Output: A committed `phase15-review-witness` package, a machine-readable review-bundle manifest, and tests that enforce the bundle contract.

## Context
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-parity-witness-and-regression-validation/15-CONTEXT.md
@.planning/phases/14-text-and-illustration-fidelity-for-parity-scenes/14-03-SUMMARY.md
@artifacts/source-parity-demo/project-engine.json
@artifacts/source-parity-demo/check/phase14-review-targets.json
@artifacts/source-parity-demo/out/phase14-fidelity-witness/frame-manifest.json
@tests/Whiteboard.Cli.Tests/ParityCompositionWitnessTests.cs
@src/Whiteboard.Export/Services/ExportPipeline.cs
@src/Whiteboard.Cli/Program.cs

## Tasks
<task type="auto">
  <name>Create a deterministic Phase 15 witness-export wrapper and review-bundle manifest</name>
  <files>
artifacts/source-parity-demo/export-phase15-review-witness.ps1
artifacts/source-parity-demo/check/phase15-review-bundle.json
  </files>
  <read_first>
artifacts/source-parity-demo/project-engine.json
artifacts/source-parity-demo/check/phase14-review-targets.json
artifacts/source-parity-demo/out/phase14-fidelity-witness/frame-manifest.json
src/Whiteboard.Export/Services/ExportPipeline.cs
src/Whiteboard.Cli/Program.cs
.planning/phases/14-text-and-illustration-fidelity-for-parity-scenes/14-03-SUMMARY.md
  </read_first>
  <action>Create `artifacts/source-parity-demo/export-phase15-review-witness.ps1` as a thin wrapper around the existing CLI that renders `artifacts/source-parity-demo/project-engine.json` to `artifacts/source-parity-demo/out/phase15-review-witness`. The script must keep the canonical export-package route, set `WHITEBOARD_ENABLE_PLAYABLE_MEDIA=0` only when the caller has not already set it, and preserve env-gated playable-media encoding when both `WHITEBOARD_ENABLE_PLAYABLE_MEDIA=1` and `WHITEBOARD_FFMPEG_PATH` are present. Emit `artifacts/source-parity-demo/check/phase15-review-bundle.json` with exact top-level fields `sourceSpec`, `baselineWitness`, `reviewWitness`, `frameManifest`, `frameCount`, `anchorFrames`, `lineage`, and `playableMedia`. The bundle must point to anchor frames `27`, `72`, `93`, `130`, `185`, and `214`, keep `phase14-fidelity-witness` as the baseline witness lineage entry, and record playable-media `status` plus `relativePath` when media is produced.</action>
  <acceptance_criteria>
1. `artifacts/source-parity-demo/export-phase15-review-witness.ps1` exists and contains `project-engine.json`, `phase15-review-witness`, and `WHITEBOARD_ENABLE_PLAYABLE_MEDIA`.
2. `artifacts/source-parity-demo/check/phase15-review-bundle.json` exists and contains `"frameCount": 264`.
3. `artifacts/source-parity-demo/check/phase15-review-bundle.json` contains anchor-frame entries for `27`, `72`, `93`, `130`, `185`, and `214`.
4. `artifacts/source-parity-demo/check/phase15-review-bundle.json` contains `"baselineWitness": "out/phase14-fidelity-witness"` and `"reviewWitness": "out/phase15-review-witness"`.
5. `artifacts/source-parity-demo/check/phase15-review-bundle.json` contains a `playableMedia` object with a `status` field even when media output is skipped.
  </acceptance_criteria>
  <verify>
    <automated>$bundle = Get-Content 'artifacts/source-parity-demo/check/phase15-review-bundle.json' -Raw | ConvertFrom-Json; if ($bundle.frameCount -ne 264) { throw "Unexpected frameCount: $($bundle.frameCount)" }; @(27,72,93,130,185,214) | ForEach-Object { if (-not ($bundle.anchorFrames.frameIndex -contains $_)) { throw "Missing anchor frame $_" } }; if ($bundle.baselineWitness -ne 'out/phase14-fidelity-witness') { throw "Unexpected baseline witness: $($bundle.baselineWitness)" }; if ($bundle.reviewWitness -ne 'out/phase15-review-witness') { throw "Unexpected review witness: $($bundle.reviewWitness)" }; if (-not $bundle.playableMedia.status) { throw 'Missing playableMedia.status.' }</automated>
  </verify>
  <done>The repo has one deterministic review-bundle contract that points reviewers at the authored witness package and its anchor frames without adding a new CLI mode.</done>
</task>

<task type="auto">
  <name>Add review-bundle tests and commit the Phase 15 review witness package</name>
  <files>
tests/Whiteboard.Cli.Tests/ParityWitnessReviewBundleTests.cs
tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj
artifacts/source-parity-demo/out/phase15-review-witness/*
.planning/phases/15-parity-witness-and-regression-validation/15-01-SUMMARY.md
  </files>
  <read_first>
tests/Whiteboard.Cli.Tests/ParityCompositionWitnessTests.cs
tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj
artifacts/source-parity-demo/check/phase15-review-bundle.json
artifacts/source-parity-demo/check/phase14-review-targets.json
artifacts/source-parity-demo/out/phase14-fidelity-witness/frame-manifest.json
artifacts/source-parity-demo/export-phase15-review-witness.ps1
  </read_first>
  <action>Create `tests/Whiteboard.Cli.Tests/ParityWitnessReviewBundleTests.cs` and add it to `tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj`. The tests must load `artifacts/source-parity-demo/check/phase15-review-bundle.json`, render `artifacts/source-parity-demo/project-engine.json` through `PipelineOrchestrator`, and assert that the exported package keeps `frame-manifest.json`, `frameCount` `264`, and anchor-frame artifact paths for `frame-000027.svg`, `frame-000072.svg`, `frame-000093.svg`, `frame-000130.svg`, `frame-000185.svg`, and `frame-000214.svg`. The test should also assert `PlayableMediaStatus` is `not-configured` when the media env vars are absent and only expect a media path when the bundle reports a produced media artifact. After the tests pass, run `artifacts/source-parity-demo/export-phase15-review-witness.ps1` to generate and commit `artifacts/source-parity-demo/out/phase15-review-witness`, then record the output paths, anchor frames, and playable-media status in `15-01-SUMMARY.md`.</action>
  <acceptance_criteria>
1. `tests/Whiteboard.Cli.Tests/ParityWitnessReviewBundleTests.cs` exists and reads `artifacts/source-parity-demo/check/phase15-review-bundle.json`.
2. `tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj` includes `ParityWitnessReviewBundleTests.cs`.
3. `artifacts/source-parity-demo/out/phase15-review-witness/frame-manifest.json` exists after running the export wrapper.
4. `artifacts/source-parity-demo/out/phase15-review-witness/frames/frame-000027.svg` exists.
5. `artifacts/source-parity-demo/out/phase15-review-witness/frames/frame-000214.svg` exists.
6. `.planning/phases/15-parity-witness-and-regression-validation/15-01-SUMMARY.md` records the witness output path and playable-media status.
7. `dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~ParityWitnessReviewBundleTests"` passes.
  </acceptance_criteria>
  <verify>
    <automated>$env:WHITEBOARD_ENABLE_PLAYABLE_MEDIA = '0'; dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~ParityWitnessReviewBundleTests"; powershell -ExecutionPolicy Bypass -File artifacts/source-parity-demo/export-phase15-review-witness.ps1; if (-not (Test-Path 'artifacts/source-parity-demo/out/phase15-review-witness/frame-manifest.json')) { throw 'Missing Phase 15 review witness manifest.' }; if (-not (Test-Path 'artifacts/source-parity-demo/out/phase15-review-witness/frames/frame-000027.svg')) { throw 'Missing frame-000027.svg.' }; if (-not (Test-Path 'artifacts/source-parity-demo/out/phase15-review-witness/frames/frame-000214.svg')) { throw 'Missing frame-000214.svg.' }; $summary = Get-Content '.planning/phases/15-parity-witness-and-regression-validation/15-01-SUMMARY.md' -Raw; if ($summary -notmatch 'artifacts/source-parity-demo/out/phase15-review-witness') { throw 'Summary missing witness output path.' }; if ($summary -notmatch 'Playable media status') { throw 'Summary missing playable-media status.' }</automated>
  </verify>
  <done>Phase 15 has a committed review witness directory and a test-backed manifest that points reviewers at the exact parity artifacts they should inspect.</done>
</task>

## Verification
```powershell
$env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'
dotnet build 'whiteboard-engine.sln' --no-restore -v minimal /m:1
$env:WHITEBOARD_ENABLE_PLAYABLE_MEDIA = '0'
dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~ParityCompositionWitnessTests|FullyQualifiedName~ParityWitnessReviewBundleTests"
powershell -ExecutionPolicy Bypass -File artifacts/source-parity-demo/export-phase15-review-witness.ps1
Test-Path 'artifacts/source-parity-demo/out/phase15-review-witness/frame-manifest.json'
```

## Success Criteria
- `AST-02` is satisfied through one stable review bundle rooted in the export-package witness path.
- Reviewers can find the committed manifest, anchor frames, and media status without reading console deterministic-key dumps.
- The Phase 14 witness lineage remains explicit inside the Phase 15 review contract.

## Output
After completion, create `.planning/phases/15-parity-witness-and-regression-validation/15-01-SUMMARY.md`.
