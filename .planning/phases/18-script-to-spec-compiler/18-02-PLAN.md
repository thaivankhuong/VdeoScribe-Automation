---
phase: 18-script-to-spec-compiler
plan: 02
title: Compile deterministic script input into validated project spec JSON through Core and thin CLI orchestration
type: execute
wave: 2
depends_on: [18-01]
requirements: [CMP-01]
files_modified:
  - src/Whiteboard.Core/Compilation/IScriptCompiler.cs
  - src/Whiteboard.Core/Compilation/ScriptCompileResult.cs
  - src/Whiteboard.Core/Compilation/ScriptCompiler.cs
  - src/Whiteboard.Cli/Models/CliScriptCompileCommandRequest.cs
  - src/Whiteboard.Cli/Contracts/IScriptCompilationOrchestrator.cs
  - src/Whiteboard.Cli/Services/ScriptCompilationOrchestrator.cs
  - src/Whiteboard.Cli/Services/CliCommandParser.cs
  - src/Whiteboard.Cli/Program.cs
  - tests/Whiteboard.Core.Tests/ScriptCompilerTests.cs
  - tests/Whiteboard.Cli.Tests/ScriptCompilationOrchestratorTests.cs
  - tests/Whiteboard.Cli.Tests/CliCommandParserTests.cs
  - tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj
  - tests/Whiteboard.Cli.Tests/Fixtures/phase18-script-compiler/script-valid.json
  - .planning/phases/18-script-to-spec-compiler/18-02-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - A CLI compile command can convert structured script JSON into a valid `VideoProject` spec artifact without manual spec editing per CMP-01.
    - Compiled spec output is assembled only from catalog-backed templates plus governed assets/effect profiles, then validated through the existing Core spec pipeline per D-07 and D-08.
    - Successful compile output is deterministic for equivalent input and stops at spec/report artifacts without invoking render or export flows per D-09.
  artifacts:
    - src/Whiteboard.Core/Compilation/ScriptCompiler.cs
    - src/Whiteboard.Cli/Services/ScriptCompilationOrchestrator.cs
    - src/Whiteboard.Cli/Services/CliCommandParser.cs
    - src/Whiteboard.Cli/Program.cs
    - tests/Whiteboard.Cli.Tests/Fixtures/phase18-script-compiler/script-valid.json
    - tests/Whiteboard.Cli.Tests/ScriptCompilationOrchestratorTests.cs
  key_links:
    - src/Whiteboard.Core/Compilation/ScriptCompiler.cs -> src/Whiteboard.Core/Compilation/ScriptMappingPipeline.cs via ordered section compilation plans from 18-01
    - src/Whiteboard.Core/Compilation/ScriptCompiler.cs -> src/Whiteboard.Core/Templates/TemplateComposer.cs via `TemplateInstantiationRequest` composition
    - src/Whiteboard.Core/Compilation/ScriptCompiler.cs -> src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs via required spec validation before success
    - src/Whiteboard.Cli/Services/ScriptCompilationOrchestrator.cs -> src/Whiteboard.Cli/Services/CliCommandParser.cs via `script compile --input --spec-output`
---

## Objective
Implement the compile path from script document to project spec artifact.

Purpose: complete the executable path for `CMP-01` by compiling the ordered section plan from 18-01 into a valid `VideoProject` JSON document, validating it through existing Core normalization/semantic checks, and exposing it through a thin CLI command.
Output: a reusable Core compiler service, CLI compile command/request models, one committed valid fixture, and deterministic Core/CLI tests.

## Context
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/18-script-to-spec-compiler/18-CONTEXT.md
@.planning/phases/18-script-to-spec-compiler/18-01-PLAN.md
@.planning/phases/17-template-contracts-and-scene-composition/17-02-SUMMARY.md
@.planning/phases/17-template-contracts-and-scene-composition/17-03-SUMMARY.md
@.planning/script-compiler/template-mappings.json
@.planning/script-compiler/governed-library.json
@src/Whiteboard.Core/Compilation/ScriptCompilationPlan.cs
@src/Whiteboard.Core/Compilation/IScriptMappingPipeline.cs
@src/Whiteboard.Core/Templates/ITemplateComposer.cs
@src/Whiteboard.Core/Templates/TemplateComposer.cs
@src/Whiteboard.Core/Models/VideoProject.cs
@src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs
@src/Whiteboard.Cli/Services/TemplatePipelineOrchestrator.cs
@src/Whiteboard.Cli/Services/CliCommandParser.cs
@src/Whiteboard.Cli/Program.cs
@tests/Whiteboard.Cli.Tests/TemplatePipelineOrchestratorTests.cs

## Interfaces
<interfaces>
From `src/Whiteboard.Core/Models/VideoProject.cs`:

```csharp
public record VideoProject
{
    public ProjectMeta Meta { get; init; } = new();
    public OutputSpec Output { get; init; } = new();
    public AssetCollection Assets { get; init; } = new();
    public List<SceneDefinition> Scenes { get; init; } = [];
    public TimelineDefinition Timeline { get; init; } = new();
}
```

From `src/Whiteboard.Core/Templates/ITemplateComposer.cs`:

```csharp
public interface ITemplateComposer
{
    TemplateInstantiationResult Compose(TemplateInstantiationRequest request);
}
```

From `src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs`:

```csharp
public sealed class SpecProcessingPipeline : ISpecProcessingPipeline
{
    public SpecProcessingResult Process(string json, string sourcePath)
```

The new compiler should produce a `VideoProject` plus canonical JSON, then prove success by passing `SpecProcessingPipeline.Process`. CLI code should stay at argument parsing, file IO, and result presentation only.
</interfaces>

## Tasks
<task type="auto">
  <name>Implement the Core script compiler and validated project-spec assembly</name>
  <files>
src/Whiteboard.Core/Compilation/IScriptCompiler.cs
src/Whiteboard.Core/Compilation/ScriptCompileResult.cs
src/Whiteboard.Core/Compilation/ScriptCompiler.cs
tests/Whiteboard.Core.Tests/ScriptCompilerTests.cs
  </files>
  <read_first>
.planning/phases/18-script-to-spec-compiler/18-01-PLAN.md
.planning/script-compiler/template-mappings.json
.planning/script-compiler/governed-library.json
src/Whiteboard.Core/Compilation/ScriptCompilationPlan.cs
src/Whiteboard.Core/Compilation/IScriptMappingPipeline.cs
src/Whiteboard.Core/Templates/ITemplateComposer.cs
src/Whiteboard.Core/Templates/TemplateComposer.cs
src/Whiteboard.Core/Models/VideoProject.cs
src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs
tests/Whiteboard.Core.Tests/TemplateComposerTests.cs
tests/Whiteboard.Core.Tests/SpecProcessingPipelineTests.cs
  </read_first>
  <action>Create `IScriptCompiler` with a `Compile(string json, string sourcePath, string templateCatalogPath, string mappingCatalogPath, string governedLibraryPath)` entry point and implement it in `ScriptCompiler`. The compiler must call the 18-01 mapping pipeline, resolve each section through `TemplateComposer` using the mapped `TemplateInstantiationRequest`, and merge the resulting scenes and timeline events into one `VideoProject`. Set `VideoProject.Meta.ProjectId` from `scriptId`, `Meta.Name` from `projectName`, `Meta.AssetRegistrySnapshotId` from the script document, and populate `Assets.RegistrySnapshot`, `Assets.SvgAssets`, and `Timeline.EffectProfiles` from `.planning/script-compiler/governed-library.json` using only the asset/effect IDs actually referenced by compiled sections. Preserve deterministic ordering: scenes by section `order` then `sectionId`, scene objects by existing template normalization, timeline events by `sceneId`, `startSeconds`, and `id`, assets/effect profiles by ordinal ID. Serialize canonical JSON and run it through `SpecProcessingPipeline.Process`; if any spec validation issue remains, the compile result must fail instead of writing a spec. Add `ScriptCompilerTests` that prove successful compilation of two ordered sections into a valid spec, repeated-run canonical JSON equivalence, and rejection when generated spec semantics fail validation.</action>
  <acceptance_criteria>
1. `src/Whiteboard.Core/Compilation/ScriptCompiler.cs` contains `SpecProcessingPipeline` usage before success is returned.
2. `src/Whiteboard.Core/Compilation/ScriptCompiler.cs` populates `Meta.ProjectId`, `Meta.Name`, `Meta.AssetRegistrySnapshotId`, `Assets.RegistrySnapshot`, `Assets.SvgAssets`, and `Timeline.EffectProfiles`.
3. `src/Whiteboard.Core/Compilation/ScriptCompiler.cs` uses `TemplateComposer` rather than building scenes/timeline events by hand.
4. `tests/Whiteboard.Core.Tests/ScriptCompilerTests.cs` asserts valid spec generation for two ordered sections.
5. `tests/Whiteboard.Core.Tests/ScriptCompilerTests.cs` asserts equivalent input produces identical canonical JSON or deterministic key across repeated runs.
6. `dotnet test 'tests/Whiteboard.Core.Tests/Whiteboard.Core.Tests.csproj' -v minimal --filter "FullyQualifiedName~ScriptCompilerTests"` passes.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path 'src/Whiteboard.Core/Compilation/ScriptCompiler.cs' -Pattern 'SpecProcessingPipeline|Meta\\.ProjectId|Meta\\.Name|AssetRegistrySnapshotId|Assets\\.RegistrySnapshot|Assets\\.SvgAssets|Timeline\\.EffectProfiles|TemplateComposer' | Out-Null; Select-String -Path 'tests/Whiteboard.Core.Tests/ScriptCompilerTests.cs' -Pattern 'ordered sections|DeterministicKey|CanonicalJson|SpecProcessingPipeline' | Out-Null; dotnet test 'tests/Whiteboard.Core.Tests/Whiteboard.Core.Tests.csproj' -v minimal --filter \"FullyQualifiedName~ScriptCompilerTests\"</automated>
  </verify>
  <done>Core can compile deterministic script JSON into a valid, normalized project spec without routing business logic through the CLI.</done>
</task>

<task type="auto">
  <name>Add the `script compile` CLI command and fixture-backed spec-output coverage</name>
  <files>
src/Whiteboard.Cli/Models/CliScriptCompileCommandRequest.cs
src/Whiteboard.Cli/Contracts/IScriptCompilationOrchestrator.cs
src/Whiteboard.Cli/Services/ScriptCompilationOrchestrator.cs
src/Whiteboard.Cli/Services/CliCommandParser.cs
src/Whiteboard.Cli/Program.cs
tests/Whiteboard.Cli.Tests/ScriptCompilationOrchestratorTests.cs
tests/Whiteboard.Cli.Tests/CliCommandParserTests.cs
tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj
tests/Whiteboard.Cli.Tests/Fixtures/phase18-script-compiler/script-valid.json
.planning/phases/18-script-to-spec-compiler/18-02-SUMMARY.md
  </files>
  <read_first>
src/Whiteboard.Core/Compilation/IScriptCompiler.cs
src/Whiteboard.Core/Compilation/ScriptCompileResult.cs
src/Whiteboard.Cli/Models/CliTemplateCommandRequest.cs
src/Whiteboard.Cli/Contracts/ITemplatePipelineOrchestrator.cs
src/Whiteboard.Cli/Services/TemplatePipelineOrchestrator.cs
src/Whiteboard.Cli/Services/CliCommandParser.cs
src/Whiteboard.Cli/Program.cs
tests/Whiteboard.Cli.Tests/TemplatePipelineOrchestratorTests.cs
tests/Whiteboard.Cli.Tests/CliCommandParserTests.cs
tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj
  </read_first>
  <action>Create `CliScriptCompileCommandRequest.cs` with request/result records for one CLI operation: `script compile --input <path> --spec-output <path>`. Add `IScriptCompilationOrchestrator` plus `ScriptCompilationOrchestrator` that loads the input file, calls `IScriptCompiler`, and writes the validated project spec JSON to the exact `--spec-output` path. Extend `CliCommandParser` and `Program` to support a new `CliCommandMode.ScriptCompile` while keeping existing `run`, `batch`, and `template` flows untouched. The CLI must print `Success`, `ScriptId`, `TemplateCount`, `SectionCount`, `SpecOutputPath`, and `DeterministicKey` on success, and it must not invoke render/export code paths per D-09. Add `tests/Whiteboard.Cli.Tests/Fixtures/phase18-script-compiler/script-valid.json` with at least two ordered sections. Add `ScriptCompilationOrchestratorTests` for successful compile/output file creation and deterministic repeated-run output, then update `CliCommandParserTests` plus `Whiteboard.Cli.Tests.csproj` compile includes for the new CLI sources.</action>
  <acceptance_criteria>
1. `src/Whiteboard.Cli/Services/CliCommandParser.cs` recognizes `script compile`.
2. `src/Whiteboard.Cli/Program.cs` prints `ScriptId`, `SectionCount`, `SpecOutputPath`, and `DeterministicKey` for `script compile`.
3. `src/Whiteboard.Cli/Services/ScriptCompilationOrchestrator.cs` writes a spec JSON file and does not call batch/render/export orchestrators.
4. `tests/Whiteboard.Cli.Tests/Fixtures/phase18-script-compiler/script-valid.json` exists and contains two section entries.
5. `tests/Whiteboard.Cli.Tests/ScriptCompilationOrchestratorTests.cs` asserts output file creation and deterministic repeated-run equivalence.
6. `tests/Whiteboard.Cli.Tests/CliCommandParserTests.cs` asserts parsing for `script compile --input --spec-output`.
7. `dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' -v minimal --filter "FullyQualifiedName~ScriptCompilationOrchestratorTests|FullyQualifiedName~CliCommandParserTests"` passes.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path 'src/Whiteboard.Cli/Services/CliCommandParser.cs' -Pattern 'script|compile|ScriptCompile' | Out-Null; Select-String -Path 'src/Whiteboard.Cli/Program.cs' -Pattern 'ScriptId|SectionCount|SpecOutputPath|DeterministicKey' | Out-Null; Select-String -Path 'tests/Whiteboard.Cli.Tests/Fixtures/phase18-script-compiler/script-valid.json' -Pattern '"sections"' | Out-Null; Select-String -Path 'tests/Whiteboard.Cli.Tests/ScriptCompilationOrchestratorTests.cs' -Pattern 'DeterministicKey|SpecOutputPath|ScriptId' | Out-Null; Select-String -Path 'tests/Whiteboard.Cli.Tests/CliCommandParserTests.cs' -Pattern 'script compile|spec-output' | Out-Null; dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' -v minimal --filter \"FullyQualifiedName~ScriptCompilationOrchestratorTests|FullyQualifiedName~CliCommandParserTests\"</automated>
  </verify>
  <done>Operators can compile script JSON into a validated spec artifact through a thin CLI command without manual spec assembly or downstream render invocation.</done>
</task>

## Verification
```powershell
$env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'
dotnet test 'tests/Whiteboard.Core.Tests/Whiteboard.Core.Tests.csproj' -v minimal --filter "FullyQualifiedName~ScriptCompilerTests"
dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' -v minimal --filter "FullyQualifiedName~ScriptCompilationOrchestratorTests|FullyQualifiedName~CliCommandParserTests"
```

## Success Criteria
- `CMP-01` is executable through `script compile --input --spec-output`.
- Successful compile output is a valid `VideoProject` JSON document validated through existing Core spec processing, not a parallel compiler-only schema.
- CLI orchestration remains thin and deterministic, stopping at compile artifacts per D-07 through D-09.

## Output
After completion, create `.planning/phases/18-script-to-spec-compiler/18-02-SUMMARY.md`.
