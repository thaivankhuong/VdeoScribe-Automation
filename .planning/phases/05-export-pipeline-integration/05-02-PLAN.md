---
phase: 5
type: plan
plan: "05-02"
title: Add repeatability checks for export outputs
wave: 2
depends_on:
  - "05-01"
files_modified:
  - tests/Whiteboard.Cli.Tests/ExportPipelineContractTests.cs
  - tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
  - tests/Whiteboard.Cli.Tests/Fixtures/phase05-export-packaging/primary-spec.json
  - tests/Whiteboard.Cli.Tests/Fixtures/phase05-export-packaging/equivalent-reordered-spec.json
  - tests/Whiteboard.Engine.Tests/FrameStateResolverContractTests.cs
autonomous: true
requirements:
  - PIPE-02
  - PIPE-03
must_haves:
  truths:
    - Repeat runs with identical normalized inputs produce identical export package metadata and deterministic export signatures.
    - Equivalent reordered specs with the same semantic frame/audio content produce identical export package contents end to end.
    - Export verification proves packaging preserves renderer semantics and timing/audio metadata rather than mutating or dropping them.
  artifacts:
    - tests/Whiteboard.Cli.Tests/ExportPipelineContractTests.cs
    - tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
    - tests/Whiteboard.Cli.Tests/Fixtures/phase05-export-packaging/primary-spec.json
    - tests/Whiteboard.Cli.Tests/Fixtures/phase05-export-packaging/equivalent-reordered-spec.json
    - tests/Whiteboard.Engine.Tests/FrameStateResolverContractTests.cs
  key_links:
    - tests/Whiteboard.Cli.Tests/ExportPipelineContractTests.cs -> src/Whiteboard.Export/Services/ExportPipeline.cs via frame/audio package parity and missing-audio assertions
    - tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs -> src/Whiteboard.Cli/Services/PipelineOrchestrator.cs via end-to-end export signature parity across equivalent specs
    - tests/Whiteboard.Engine.Tests/FrameStateResolverContractTests.cs -> src/Whiteboard.Engine/Models/ResolvedFrameState.cs via preserved renderer/export handoff timing evidence
---

<objective>
Lock Phase 05 with deterministic export parity tests covering frame packaging, audio cue synchronization, and equivalent-input end-to-end repeatability.
</objective>

## Objective
Lock Phase 05 with deterministic export parity tests covering frame packaging, audio cue synchronization, and equivalent-input end-to-end repeatability.

## Constraints
- Execute only after `05-01` establishes export package contracts and audio metadata packaging.
- Keep verification deterministic and contract-focused; avoid subjective or codec-specific output checks.
- Prefer fixture-driven semantic parity rather than ad hoc assertions tied to file-system ordering.
- Keep CLI integration limited to single-run flows; do not introduce batch orchestration here.

<tasks>
  <task id="05-02-01">
    <name>Expand export contract tests for package ordering, timing, and audio failures</name>
    <action>Add export-focused tests that lock frame package ordering, timing metadata derivation, deterministic export signatures, and fail-fast behavior for missing referenced audio assets.</action>
    <files>tests/Whiteboard.Cli.Tests/ExportPipelineContractTests.cs</files>
    <verify>
      <automated>dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~ExportPipelineContractTests.Repeatability|FullyQualifiedName~ExportPipelineContractTests.MissingAudio"</automated>
    </verify>
    <done>Export contract coverage fails on timing drift, ordering drift, or missing-audio behavior regressions.</done>
  </task>
  <task id="05-02-02">
    <name>Add end-to-end parity fixtures for export package outputs</name>
    <action>Create export-focused fixture specs with audio metadata and equivalent reordered input variants, then assert identical export package contents and deterministic keys through `PipelineOrchestrator`.</action>
    <files>tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs; tests/Whiteboard.Cli.Tests/Fixtures/phase05-export-packaging/primary-spec.json; tests/Whiteboard.Cli.Tests/Fixtures/phase05-export-packaging/equivalent-reordered-spec.json</files>
    <verify>
      <automated>dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~PipelineOrchestratorIntegrationTests"</automated>
    </verify>
    <done>Equivalent specs produce identical end-to-end export package metadata and deterministic output signatures.</done>
  </task>
  <task id="05-02-03">
    <name>Lock renderer-to-export timing evidence in contract tests</name>
    <action>Strengthen contract-level tests so frame index, frame rate, and renderer-facing payload fields needed by export remain explicit and stable, preventing export from reintroducing semantic recomputation.</action>
    <files>tests/Whiteboard.Engine.Tests/FrameStateResolverContractTests.cs; tests/Whiteboard.Cli.Tests/ExportPipelineContractTests.cs; tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs</files>
    <verify>
      <automated>dotnet test "tests/Whiteboard.Engine.Tests/Whiteboard.Engine.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~FrameStateResolverContractTests"</automated>
    </verify>
    <done>Engine handoff and downstream export assertions prove export depends only on stable upstream contracts and package rules.</done>
  </task>
</tasks>

## Verification
Automated commands:
```powershell
dotnet build "whiteboard-engine.sln" --no-restore -v minimal /m:1
dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~ExportPipelineContractTests"
dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~PipelineOrchestratorIntegrationTests"
dotnet test "tests/Whiteboard.Engine.Tests/Whiteboard.Engine.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~FrameStateResolverContractTests"
```

Manual checks:
- Re-run one export fixture twice and confirm export package contents plus deterministic key remain byte-for-byte identical.
- Inspect one packaged audio cue case and confirm start, duration, volume, and asset identity remain canonical and stable.
