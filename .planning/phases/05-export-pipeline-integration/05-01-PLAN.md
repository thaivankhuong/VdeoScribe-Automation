---
phase: 5
type: plan
plan: "05-01"
title: Implement export contracts and frame/audio packaging flow
wave: 1
depends_on: []
files_modified:
  - src/Whiteboard.Export/Models/ExportRequest.cs
  - src/Whiteboard.Export/Models/ExportTarget.cs
  - src/Whiteboard.Export/Models/ExportResult.cs
  - src/Whiteboard.Export/Services/ExportPipeline.cs
  - src/Whiteboard.Export/Contracts/IExportPipeline.cs
  - src/Whiteboard.Cli/Models/CliRunResult.cs
  - src/Whiteboard.Cli/Services/PipelineOrchestrator.cs
  - tests/Whiteboard.Cli.Tests/ExportPipelineContractTests.cs
autonomous: true
requirements:
  - PIPE-02
  - PIPE-03
must_haves:
  truths:
    - Export packages renderer frame results into an explicit deterministic export package that preserves frame ordering, renderer operations, and timing metadata without recomputing upstream semantics.
    - Export packages normalized audio cue metadata and fails fast deterministically when referenced audio assets are missing.
    - CLI handoff into Export passes only explicit export inputs and surfaces enough export metadata for deterministic verification.
  artifacts:
    - src/Whiteboard.Export/Models/ExportRequest.cs
    - src/Whiteboard.Export/Models/ExportTarget.cs
    - src/Whiteboard.Export/Models/ExportResult.cs
    - src/Whiteboard.Export/Services/ExportPipeline.cs
    - src/Whiteboard.Cli/Services/PipelineOrchestrator.cs
    - tests/Whiteboard.Cli.Tests/ExportPipelineContractTests.cs
  key_links:
    - src/Whiteboard.Cli/Services/PipelineOrchestrator.cs -> src/Whiteboard.Export/Models/ExportRequest.cs via explicit export handoff of frame/audio/output metadata
    - src/Whiteboard.Export/Services/ExportPipeline.cs -> src/Whiteboard.Renderer/Models/RenderFrameResult.cs via deterministic frame packaging without semantic reinterpretation
    - src/Whiteboard.Export/Services/ExportPipeline.cs -> src/Whiteboard.Core/Timeline/AudioCue.cs via normalized audio cue packaging and fail-fast asset resolution
---

<objective>
Replace placeholder export behavior with deterministic export package contracts and packaging logic for ordered frames, timing metadata, and audio cues.
</objective>

## Objective
Replace placeholder export behavior with deterministic export package contracts and packaging logic for ordered frames, timing metadata, and audio cues.

## Constraints
- Keep Export adapter-only; do not recompute draw, camera, or timeline semantics.
- Keep CLI changes limited to single-run export handoff and result surfacing.
- Avoid speculative encoder ecosystems or unnecessary dependencies.
- Preserve deterministic packaging order and fixed serialization rules across identical inputs.

<tasks>
  <task id="05-01-01">
    <name>Expand export request/result contracts for frame and audio packaging</name>
    <action>Add explicit export package models and extend export request/target/result contracts to carry ordered frame metadata, ordered audio cue metadata, target settings, and deterministic export summaries.</action>
    <files>src/Whiteboard.Export/Models/ExportRequest.cs; src/Whiteboard.Export/Models/ExportTarget.cs; src/Whiteboard.Export/Models/ExportResult.cs; tests/Whiteboard.Cli.Tests/ExportPipelineContractTests.cs</files>
    <verify>
      <automated>dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~ExportPipelineContractTests"</automated>
    </verify>
    <done>Export contracts describe deterministic frame/audio packaging inputs and outputs clearly enough for downstream verification.</done>
  </task>
  <task id="05-01-02">
    <name>Implement deterministic export packaging and audio fail-fast rules</name>
    <action>Update `ExportPipeline` to package renderer frame results by stable frame order, attach frame timing metadata, resolve normalized audio cues/assets, and fail fast deterministically when referenced audio assets are missing.</action>
    <files>src/Whiteboard.Export/Contracts/IExportPipeline.cs; src/Whiteboard.Export/Services/ExportPipeline.cs; tests/Whiteboard.Cli.Tests/ExportPipelineContractTests.cs</files>
    <verify>
      <automated>dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~ExportPipelineContractTests.FramePackaging|FullyQualifiedName~ExportPipelineContractTests.AudioPackaging"</automated>
    </verify>
    <done>Export pipeline emits deterministic package metadata and stable fail-fast behavior for audio asset resolution problems.</done>
  </task>
  <task id="05-01-03">
    <name>Integrate CLI export handoff and result surfacing</name>
    <action>Update `PipelineOrchestrator` and `CliRunResult` so the CLI passes export-relevant timing/audio inputs into Export and surfaces deterministic export package metadata without widening into batch orchestration.</action>
    <files>src/Whiteboard.Cli/Models/CliRunResult.cs; src/Whiteboard.Cli/Services/PipelineOrchestrator.cs; tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs</files>
    <verify>
      <automated>dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~PipelineOrchestratorIntegrationTests"</automated>
    </verify>
    <done>CLI end-to-end execution reports export package details and deterministic keys that include export-level semantics.</done>
  </task>
</tasks>

## Verification
Automated commands:
```powershell
dotnet build "whiteboard-engine.sln" --no-restore -v minimal /m:1
dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~ExportPipelineContractTests"
dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~PipelineOrchestratorIntegrationTests"
```

Manual checks:
- Inspect export package metadata and confirm frame ordering plus audio cue ordering are explicit and stable.
- Confirm missing audio assets stop export deterministically before any ambiguous package result is returned.
