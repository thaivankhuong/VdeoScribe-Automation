---
phase: 13-object-motion-and-hand-sequencing-parity
plan: 03
title: Validate motion and hand timing parity through frame and video witnesses
wave: 3
depends_on: [13-02]
requirements: [PAR-02, PAR-03]
files_modified:
  - artifacts/source-parity-demo/out/*
  - tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
  - tests/Whiteboard.Renderer.Tests/FrameRendererContractTests.cs
  - .planning/phases/13-object-motion-and-hand-sequencing-parity/13-03-SUMMARY.md
  - .planning/STATE.md
autonomous: true
must_haves:
  truths:
    - Repeated runs of the authored motion witness preserve deterministic package evidence.
    - Hand-follow timing remains attached to the authored parity route rather than any legacy shortcut fixture.
    - Motion and hand sequencing changes remain visible through the real CLI/export witness flow.
  artifacts:
    - artifacts/source-parity-demo/out/*
    - tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
  key_links:
    - artifacts/source-parity-demo/project-engine.json -> artifacts/source-parity-demo/out/* via authored motion witness generation
    - tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs -> witness outputs via repeated-run parity checks
---

## Objective
Prove the authored parity route still holds under full-scene motion and hand sequencing changes by generating reviewable witness artifacts and repeated-run deterministic checks.

## Tasks
<task>
  <name>Render motion-aware authored witness output end to end</name>
  <action>Run the authored witness through the real CLI/export flow after Phase 13 motion and hand-follow work, capturing frame or media evidence suitable for review.</action>
  <done>The repo contains witness output showing motion and hand sequencing on the authored parity path.</done>
</task>

<task>
  <name>Lock repeated-run parity for motion and hand timing</name>
  <action>Add assertions that compare export packages, manifests, and any chosen witness evidence across repeated authored-witness runs.</action>
  <done>Phase 13 parity behavior is deterministic enough to support later fidelity tuning.</done>
</task>

<task>
  <name>Record completion evidence and remaining fidelity gaps</name>
  <action>Document commands, artifact paths, and any still-deferred visual gaps so Phase 14 starts from verified motion/hand behavior instead of rediscovery.</action>
  <done>Phase 13 closes with auditable proof and a clean handoff into fidelity work.</done>
</task>

## Verification
```powershell
$env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'
dotnet build 'whiteboard-engine.sln' --no-restore -v minimal /m:1
$env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'
dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~PipelineOrchestratorIntegrationTests"
if (Test-Path 'artifacts/source-parity-demo/out/phase13-motion-witness') { Remove-Item 'artifacts/source-parity-demo/out/phase13-motion-witness' -Recurse -Force }
$env:WHITEBOARD_ENABLE_PLAYABLE_MEDIA = '0'
dotnet 'src/Whiteboard.Cli/bin/Debug/net8.0/Whiteboard.Cli.dll' --spec 'artifacts/source-parity-demo/project-engine.json' --output 'artifacts/source-parity-demo/out/phase13-motion-witness'
Test-Path 'artifacts/source-parity-demo/out/phase13-motion-witness/frame-manifest.json'
```