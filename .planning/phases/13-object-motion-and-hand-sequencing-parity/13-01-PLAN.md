---
phase: 13-object-motion-and-hand-sequencing-parity
plan: 01
title: Finalize object transform event semantics and parity-oriented timeline usage
wave: 1
depends_on: []
requirements: [PAR-02]
files_modified:
  - artifacts/source-parity-demo/project-engine.json
  - src/Whiteboard.Engine/Services/ObjectStateResolver.cs
  - src/Whiteboard.Engine/Services/FrameStateResolver.cs
  - tests/Whiteboard.Engine.Tests/ObjectLifecycleResolutionTests.cs
  - tests/Whiteboard.Engine.Tests/FrameStateResolverContractTests.cs
  - tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
autonomous: true
---

## Objective
Lock the authored witness scene onto explicit transform-event semantics so parity motion work starts from engine-owned `Move`/`Scale`/`Rotate`/`Fade` behavior rather than renderer-side drift or fixture ambiguity.

## Tasks
<task>
  <name>Audit authored witness timeline usage against current transform semantics</name>
  <action>Review `project-engine.json` and the current `ObjectStateResolver` rules to ensure the authored witness expresses required object motion through existing transform events instead of relying on static placement assumptions.</action>
  <done>The witness scene clearly encodes parity-oriented transform intent through the current timeline action vocabulary.</done>
</task>

<task>
  <name>Harden transform snapshot behavior where parity gaps are verified</name>
  <action>Adjust engine-side transform resolution only where the authored witness reveals a real semantic gap, keeping event ordering, easing, and deterministic rounding explicit.</action>
  <done>Frame-state output exposes stable transform snapshots suitable for parity motion iteration.</done>
</task>

<task>
  <name>Lock engine regression coverage for parity transform behavior</name>
  <action>Add or extend engine and orchestration tests so authored witness motion semantics fail fast if transform ordering, easing application, or deterministic keys drift.</action>
  <done>Phase 13 starts from an engine-protected motion baseline instead of ad hoc witness tuning.</done>
</task>

## Verification
```powershell
$env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'
dotnet build 'whiteboard-engine.sln' --no-restore -v minimal /m:1
$env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'
dotnet test 'tests/Whiteboard.Engine.Tests/Whiteboard.Engine.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~ObjectLifecycleResolutionTests|FullyQualifiedName~FrameStateResolverContractTests"
$env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'
dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~PipelineOrchestratorIntegrationTests"
```