---
phase: 13-object-motion-and-hand-sequencing-parity
plan: 02
title: Improve hand-follow behavior and object-to-object sequencing for parity scenes
wave: 2
depends_on: [13-01]
requirements: [PAR-03]
files_modified:
  - src/Whiteboard.Renderer/Services/FrameRenderer.cs
  - src/Whiteboard.Renderer/Services/FrameRenderer.Phase11.cs
  - src/Whiteboard.Renderer/Services/SvgObjectRenderer.cs
  - tests/Whiteboard.Renderer.Tests/FrameRendererContractTests.cs
  - tests/Whiteboard.Renderer.Tests/SvgObjectRendererTests.cs
  - tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
  - artifacts/source-parity-demo/project-engine.json
autonomous: true
---

## Objective
Make hand-follow behavior on the authored witness path track active object/path progression more like the reference sample without moving sequencing semantics out of the resolved frame-state pipeline.

## Tasks
<task>
  <name>Refine hand-follow selection on the authored route</name>
  <action>Update the renderer hand-guidance/hand-asset selection logic so it prefers the correct active authored object/path and transitions deterministically between objects as the witness scene advances.</action>
  <done>Hand-follow no longer behaves like a generic overlay heuristic when multiple authored objects draw in sequence.</done>
</task>

<task>
  <name>Keep sequencing compatible with transform-aware motion</name>
  <action>Ensure hand-follow pose calculations respect the resolved object transforms produced by Phase 13-01 so movement, scaling, and rotation do not desynchronize the visible hand from the active authored object.</action>
  <done>Hand-follow remains visually coherent while staying driven by deterministic state.</done>
</task>

<task>
  <name>Protect sequencing regressions with renderer and CLI coverage</name>
  <action>Add focused tests for object-to-object sequencing, active-path preference, and repeated-run stability on the authored witness path.</action>
  <done>Phase 13 hand-follow improvements become regression-resistant instead of visual one-offs.</done>
</task>

## Verification
```powershell
$env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'
dotnet build 'whiteboard-engine.sln' --no-restore -v minimal /m:1
$env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'
dotnet test 'tests/Whiteboard.Renderer.Tests/Whiteboard.Renderer.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~FrameRendererContractTests|FullyQualifiedName~SvgObjectRendererTests"
$env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'
dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~PipelineOrchestratorIntegrationTests"
```