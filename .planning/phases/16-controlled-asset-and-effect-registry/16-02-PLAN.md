---
phase: 16-controlled-asset-and-effect-registry
plan: 02
title: Define whitelisted effect profile catalog and enforce deterministic parameter bounds
type: execute
wave: 1
depends_on: []
requirements: [EFX-01, EFX-02]
files_modified:
  - src/Whiteboard.Core/Timeline/EffectProfile.cs
  - src/Whiteboard.Core/Timeline/EffectParameterBound.cs
  - src/Whiteboard.Core/Timeline/TimelineDefinition.cs
  - src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs
  - tests/Whiteboard.Core.Tests/SpecProcessingPipelineTests.cs
  - tests/Whiteboard.Cli.Tests/ProjectSpecLoaderTests.cs
  - .planning/phases/16-controlled-asset-and-effect-registry/16-02-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - Timeline events can reference only approved effect profiles instead of arbitrary free-form behavior.
    - Effect profile limits are validated before execution so render behavior cannot drift from uncontrolled parameters.
    - Invalid profile IDs and out-of-range parameters produce deterministic semantic errors.
  artifacts:
    - src/Whiteboard.Core/Timeline/EffectProfile.cs
    - src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs
    - tests/Whiteboard.Core.Tests/SpecProcessingPipelineTests.cs
  key_links:
    - src/Whiteboard.Core/Timeline/EffectProfile.cs -> src/Whiteboard.Core/Timeline/TimelineDefinition.cs via `EffectProfiles` catalog wiring
    - src/Whiteboard.Core/Timeline/TimelineDefinition.cs -> src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs via profile lookup during semantic validation
    - src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs -> tests/Whiteboard.Cli.Tests/ProjectSpecLoaderTests.cs via deterministic loader error messages
---

## Objective
Introduce a controlled effect-profile catalog and preflight range checks for timeline parameters.

Purpose: satisfy `EFX-01` and `EFX-02` without changing renderer semantics in this phase.
Output: effect profile contracts, semantic validation rules, and tests for invalid profile usage/parameter ranges.

## Context
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/Whiteboard.Core/Timeline/TimelineDefinition.cs
@src/Whiteboard.Core/Timeline/TimelineEvent.cs
@src/Whiteboard.Core/Enums/TimelineActionType.cs
@src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs
@tests/Whiteboard.Core.Tests/SpecProcessingPipelineTests.cs
@tests/Whiteboard.Cli.Tests/ProjectSpecLoaderTests.cs
@artifacts/source-parity-demo/project-engine.json

## Tasks
<task type="auto">
  <name>Add controlled effect-profile contracts to the timeline model</name>
  <files>
src/Whiteboard.Core/Timeline/EffectProfile.cs
src/Whiteboard.Core/Timeline/EffectParameterBound.cs
src/Whiteboard.Core/Timeline/TimelineDefinition.cs
  </files>
  <read_first>
src/Whiteboard.Core/Timeline/TimelineDefinition.cs
src/Whiteboard.Core/Timeline/TimelineEvent.cs
src/Whiteboard.Core/Enums/TimelineActionType.cs
src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs
  </read_first>
  <action>Create `EffectProfile` and `EffectParameterBound` timeline contracts. `EffectProfile` must include `Id`, `ActionType`, `MinDurationSeconds`, `MaxDurationSeconds`, and `ParameterBounds` (`Dictionary<string, EffectParameterBound>`). `EffectParameterBound` must include `Key`, `MinValue`, and `MaxValue`. Update `TimelineDefinition` with `public List<EffectProfile> EffectProfiles { get; init; } = [];`. Update timeline normalization in `SpecProcessingPipeline` to sort effect profiles deterministically by `Id`, then by `ActionType`, and to sort `ParameterBounds` by key using ordinal ordering.</action>
  <acceptance_criteria>
1. `src/Whiteboard.Core/Timeline/EffectProfile.cs` exists and contains `public record EffectProfile`.
2. `src/Whiteboard.Core/Timeline/EffectParameterBound.cs` exists and contains `public record EffectParameterBound`.
3. `src/Whiteboard.Core/Timeline/TimelineDefinition.cs` contains `EffectProfiles`.
4. `src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs` contains normalization logic for `EffectProfiles`.
  </acceptance_criteria>
  <verify>
    <automated>if (-not (Test-Path 'src/Whiteboard.Core/Timeline/EffectProfile.cs')) { throw 'Missing EffectProfile contract.' }; if (-not (Test-Path 'src/Whiteboard.Core/Timeline/EffectParameterBound.cs')) { throw 'Missing EffectParameterBound contract.' }; Select-String -Path 'src/Whiteboard.Core/Timeline/TimelineDefinition.cs' -Pattern 'EffectProfiles' -SimpleMatch | Out-Null; Select-String -Path 'src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs' -Pattern 'EffectProfiles' -SimpleMatch | Out-Null</automated>
  </verify>
  <done>Timeline model carries a deterministic effect catalog contract that can be validated before execution.</done>
</task>

<task type="auto">
  <name>Validate effect profile whitelist and parameter ranges during semantic processing</name>
  <files>
src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs
tests/Whiteboard.Core.Tests/SpecProcessingPipelineTests.cs
tests/Whiteboard.Cli.Tests/ProjectSpecLoaderTests.cs
.planning/phases/16-controlled-asset-and-effect-registry/16-02-SUMMARY.md
  </files>
  <read_first>
src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs
src/Whiteboard.Core/Timeline/TimelineEvent.cs
tests/Whiteboard.Core.Tests/SpecProcessingPipelineTests.cs
tests/Whiteboard.Cli.Tests/ProjectSpecLoaderTests.cs
artifacts/source-parity-demo/project-engine.json
  </read_first>
  <action>Add semantic checks so each timeline event that contains `parameters.effectProfileId` must reference a profile in `Timeline.EffectProfiles` with matching `ActionType`. Add deterministic semantic error codes: `semantic.effect_profile.missing`, `semantic.effect_profile.action_mismatch`, and `semantic.effect_profile.parameter_out_of_range`. For each numeric event parameter declared in the chosen profile's `ParameterBounds`, parse as invariant-culture double and validate `MinValue <= value <= MaxValue`; emit `semantic.effect_profile.parameter_out_of_range` with the exact parameter key in the message when violated. Extend `SpecProcessingPipelineTests` and `ProjectSpecLoaderTests` to cover missing profile, mismatched action type, and out-of-range parameter paths. Summarize behavior and commands in `16-02-SUMMARY.md`.</action>
  <acceptance_criteria>
1. `src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs` contains `semantic.effect_profile.missing`.
2. `src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs` contains `semantic.effect_profile.action_mismatch`.
3. `src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs` contains `semantic.effect_profile.parameter_out_of_range`.
4. `tests/Whiteboard.Core.Tests/SpecProcessingPipelineTests.cs` includes assertions for all three effect-profile error codes.
5. `tests/Whiteboard.Cli.Tests/ProjectSpecLoaderTests.cs` includes at least one assertion for `semantic.effect_profile.parameter_out_of_range`.
6. `dotnet test 'tests/Whiteboard.Core.Tests/Whiteboard.Core.Tests.csproj' -v minimal --filter "FullyQualifiedName~SpecProcessingPipelineTests"` passes.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path 'src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs' -Pattern 'semantic.effect_profile.missing' -SimpleMatch | Out-Null; Select-String -Path 'src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs' -Pattern 'semantic.effect_profile.action_mismatch' -SimpleMatch | Out-Null; Select-String -Path 'src/Whiteboard.Core/Validation/SpecProcessingPipeline.cs' -Pattern 'semantic.effect_profile.parameter_out_of_range' -SimpleMatch | Out-Null; dotnet test 'tests/Whiteboard.Core.Tests/Whiteboard.Core.Tests.csproj' -v minimal --filter "FullyQualifiedName~SpecProcessingPipelineTests"</automated>
  </verify>
  <done>Whitelisted effect profiles and parameter bounds are enforced deterministically before rendering starts.</done>
</task>

## Verification
```powershell
$env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'
dotnet build 'whiteboard-engine.sln' --no-restore -v minimal /m:1
dotnet test 'tests/Whiteboard.Core.Tests/Whiteboard.Core.Tests.csproj' -v minimal --filter "FullyQualifiedName~SpecProcessingPipelineTests"
dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build -v minimal --filter "FullyQualifiedName~ProjectSpecLoaderTests"
```

## Success Criteria
- `EFX-01` is satisfied by explicit profile whitelisting in timeline contracts.
- `EFX-02` is satisfied by deterministic parameter bounds checks and stable error reporting.
- Invalid effect profile references are blocked before timeline execution.

## Output
After completion, create `.planning/phases/16-controlled-asset-and-effect-registry/16-02-SUMMARY.md`.
