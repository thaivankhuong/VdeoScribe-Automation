---
phase: 19-batch-automation-orchestrator
plan: 01
title: Implement ordered script-driven batch orchestration through existing compile and pipeline contracts
type: execute
wave: 1
depends_on: []
requirements: [AUT-01]
files_modified:
  - src/Whiteboard.Cli/Models/CliBatchManifest.cs
  - src/Whiteboard.Cli/Models/CliBatchJob.cs
  - src/Whiteboard.Cli/Services/BatchPipelineOrchestrator.cs
  - src/Whiteboard.Cli/Services/CliCommandParser.cs
  - src/Whiteboard.Cli/Program.cs
  - tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorTests.cs
  - tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorIntegrationTests.cs
  - tests/Whiteboard.Cli.Tests/CliCommandParserTests.cs
  - tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj
  - tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/primary-manifest.json
  - tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/equivalent-reordered-manifest.json
  - tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/job-a-script.json
  - tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/job-b-script.json
  - .planning/phases/19-batch-automation-orchestrator/19-01-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - Batch CLI can execute ordered script-driven jobs end to end through `script -> spec -> render -> export` without manual spec editing per D-01 and D-02.
    - Scripted batch orchestration stays thin in the CLI layer and reuses existing compile and run contracts instead of recomputing compiler, engine, renderer, or export semantics per D-03 and D-07.
    - Reordered manifest entries stay intentionally ordered by manifest position rather than canonical `jobId` sorting so operators can control deterministic run order per D-01.
  artifacts:
    - src/Whiteboard.Cli/Models/CliBatchManifest.cs
    - src/Whiteboard.Cli/Models/CliBatchJob.cs
    - src/Whiteboard.Cli/Services/BatchPipelineOrchestrator.cs
    - tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/primary-manifest.json
    - tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorIntegrationTests.cs
  key_links:
    - src/Whiteboard.Cli/Services/BatchPipelineOrchestrator.cs -> src/Whiteboard.Cli/Services/ScriptCompilationOrchestrator.cs via deterministic compile stage handoff
    - src/Whiteboard.Cli/Services/BatchPipelineOrchestrator.cs -> src/Whiteboard.Cli/Services/PipelineOrchestrator.cs via compiled spec path handoff into render/export
    - src/Whiteboard.Cli/Services/CliCommandParser.cs -> src/Whiteboard.Cli/Models/CliBatchManifest.cs via file-driven `batch --manifest --summary-output` contract
    - tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/equivalent-reordered-manifest.json -> tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorIntegrationTests.cs via preserved manifest ordering assertions
---

## Objective
Implement the executable Phase 19 batch path for script-driven production runs.

Purpose: satisfy `AUT-01` by wiring the existing Phase 18 compiler and the existing render/export pipeline into one ordered batch orchestration flow that stays file-driven, deterministic, and CLI-thin.
Output: scripted batch manifest/job contracts, deterministic job-workspace conventions, CLI routing/help updates, and fixture-backed orchestration coverage proving `script -> spec -> render -> export` without manual edits.

## Context
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/19-batch-automation-orchestrator/19-CONTEXT.md
@.planning/phases/18-script-to-spec-compiler/18-02-SUMMARY.md
@.planning/phases/18-script-to-spec-compiler/18-03-SUMMARY.md
@src/Whiteboard.Cli/Models/CliBatchManifest.cs
@src/Whiteboard.Cli/Models/CliBatchJob.cs
@src/Whiteboard.Cli/Models/CliBatchRunRequest.cs
@src/Whiteboard.Cli/Services/BatchPipelineOrchestrator.cs
@src/Whiteboard.Cli/Services/ScriptCompilationOrchestrator.cs
@src/Whiteboard.Cli/Services/PipelineOrchestrator.cs
@src/Whiteboard.Cli/Services/CliCommandParser.cs
@src/Whiteboard.Cli/Program.cs
@tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorTests.cs

## Interfaces
<interfaces>
From `src/Whiteboard.Cli/Contracts/IScriptCompilationOrchestrator.cs`:

```csharp
public interface IScriptCompilationOrchestrator
{
    CliScriptCompileCommandResult Compile(CliScriptCompileCommandRequest request);
}
```

From `src/Whiteboard.Cli/Contracts/IPipelineOrchestrator.cs`:

```csharp
public interface IPipelineOrchestrator
{
    CliRunResult Run(CliRunRequest request);
}
```

From `src/Whiteboard.Cli/Models/CliBatchRunRequest.cs`:

```csharp
public sealed record CliBatchRunRequest
{
    public string ManifestPath { get; init; } = string.Empty;
    public string SummaryOutputPath { get; init; } = string.Empty;
}
```

Reuse these contracts directly. Phase 19 orchestration may stage files and bridge outputs, but it must not duplicate compile/report assembly or renderer/export semantics in the batch layer.
</interfaces>

## Tasks
<task type="auto">
  <name>Define the scripted batch manifest contract and preserve explicit manifest ordering</name>
  <files>
src/Whiteboard.Cli/Models/CliBatchManifest.cs
src/Whiteboard.Cli/Models/CliBatchJob.cs
src/Whiteboard.Cli/Services/CliCommandParser.cs
src/Whiteboard.Cli/Program.cs
tests/Whiteboard.Cli.Tests/CliCommandParserTests.cs
tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj
tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/primary-manifest.json
tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/equivalent-reordered-manifest.json
tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/job-a-script.json
tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/job-b-script.json
  </files>
  <read_first>
.planning/phases/19-batch-automation-orchestrator/19-CONTEXT.md
.planning/phases/18-script-to-spec-compiler/18-02-SUMMARY.md
.planning/phases/18-script-to-spec-compiler/18-03-SUMMARY.md
src/Whiteboard.Cli/Models/CliBatchManifest.cs
src/Whiteboard.Cli/Models/CliBatchJob.cs
src/Whiteboard.Cli/Services/CliCommandParser.cs
src/Whiteboard.Cli/Program.cs
tests/Whiteboard.Cli.Tests/CliCommandParserTests.cs
tests/Whiteboard.Cli.Tests/Fixtures/phase18-script-compiler/script-valid.json
  </read_first>
  <action>Expand the current batch input contract into a scripted Phase 19 manifest per D-01 and D-02. Keep the entrypoint file-driven through `batch --manifest --summary-output`; do not add prompts, UI inputs, or ad hoc stdin job definitions. Update `CliBatchJob` so scripted jobs use `jobId`, `scriptPath`, and `outputPath` as the Phase 19 contract, while any retained `specPath` support stays a compatibility passthrough only and must not gain new semantics. Preserve the manifest array order as the source of truth instead of sorting by `jobId`; the ordered job list is now explicit operator intent per D-01. Add Phase 19 fixtures with two equivalent script jobs declared in opposite order so tests can prove the orchestrator preserves manifest order rather than canonicalizing it. Update parser/help text to describe scripted batch runs as compile-backed orchestration into the existing pipeline, and keep CLI responsibility limited to parsing/file routing per D-03.</action>
  <acceptance_criteria>
1. `src/Whiteboard.Cli/Models/CliBatchJob.cs` contains `JobId`, `ScriptPath`, and `OutputPath` for scripted batch jobs.
2. `src/Whiteboard.Cli/Services/CliCommandParser.cs` still accepts `batch --manifest --summary-output` and does not introduce interactive input handling.
3. `tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/primary-manifest.json` and `equivalent-reordered-manifest.json` both exist and reference the same two script fixtures in opposite order.
4. `tests/Whiteboard.Cli.Tests/CliCommandParserTests.cs` covers successful parsing of the Phase 19 batch command shape.
5. `src/Whiteboard.Cli/Program.cs` help output describes batch runs as script-driven orchestration, not direct editor/UI work.
6. No Phase 19 fixture or parser path reintroduces template file paths, runtime prompts, or job reordering by `jobId`.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path 'src/Whiteboard.Cli/Models/CliBatchJob.cs' -Pattern 'ScriptPath|OutputPath|JobId' | Out-Null; Select-String -Path 'src/Whiteboard.Cli/Services/CliCommandParser.cs' -Pattern 'batch|--manifest|--summary-output' | Out-Null; if (-not (Test-Path 'tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/primary-manifest.json')) { throw 'Missing primary Phase 19 manifest fixture.' }; if (-not (Test-Path 'tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/equivalent-reordered-manifest.json')) { throw 'Missing reordered Phase 19 manifest fixture.' }; if (-not (Test-Path 'tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/job-a-script.json')) { throw 'Missing Phase 19 job-a script fixture.' }; if (-not (Test-Path 'tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/job-b-script.json')) { throw 'Missing Phase 19 job-b script fixture.' }; Select-String -Path 'tests/Whiteboard.Cli.Tests/CliCommandParserTests.cs' -Pattern 'batch|manifest|summary-output' | Out-Null; $env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'; $env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'; dotnet msbuild 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' /t:Build /restore /m:1 /v:minimal; dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build --no-restore -v minimal --filter \"FullyQualifiedName~CliCommandParserTests\"</automated>
  </verify>
  <done>The repository has a concrete scripted batch manifest contract that preserves declared job order and keeps CLI input file-driven per D-01 through D-03.</done>
</task>

<task type="auto">
  <name>Wire `script -> spec -> render -> export` orchestration through deterministic job workspaces</name>
  <files>
src/Whiteboard.Cli/Services/BatchPipelineOrchestrator.cs
tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorTests.cs
tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorIntegrationTests.cs
tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj
.planning/phases/19-batch-automation-orchestrator/19-01-SUMMARY.md
  </files>
  <read_first>
.planning/phases/19-batch-automation-orchestrator/19-CONTEXT.md
.planning/phases/18-script-to-spec-compiler/18-02-SUMMARY.md
.planning/phases/18-script-to-spec-compiler/18-03-SUMMARY.md
src/Whiteboard.Cli/Services/BatchPipelineOrchestrator.cs
src/Whiteboard.Cli/Services/ScriptCompilationOrchestrator.cs
src/Whiteboard.Cli/Services/PipelineOrchestrator.cs
tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorTests.cs
tests/Whiteboard.Cli.Tests/ScriptCompilationOrchestratorTests.cs
tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
tests/Whiteboard.Cli.Tests/Fixtures/phase19-batch-automation/primary-manifest.json
  </read_first>
  <action>Update `BatchPipelineOrchestrator` so Phase 19 scripted jobs execute the fixed stage order `script -> spec -> render -> export` per D-02 by calling `IScriptCompilationOrchestrator` first and `IPipelineOrchestrator` second. Keep the batch layer thin per D-03 and D-07: it may derive deterministic file paths and bridge request/result objects, but it must not recompute compile reports, timeline semantics, renderer inputs, or export packaging. For scripted jobs, derive a deterministic workspace under the summary output directory using the manifest sequence and job ID, with exact relative paths `jobs/{index:000}-{jobId}/compiled-spec.json` and `jobs/{index:000}-{jobId}/compile-report.json`; do not use timestamps, temp GUID folders, or hidden side channels. Preserve declared manifest order when executing jobs. If compile fails, stop that job before render/export and surface the compile diagnostics deterministically without mutation; do not add retry logic or per-job manifest/status schemas yet, because D-06 and D-08 belong to 19-02 and Phase 20 respectively. Add focused orchestrator tests with fakes plus one integration test that uses the real Phase 18 compiler and the existing pipeline to prove a script fixture can reach export artifacts without manual spec editing.</action>
  <acceptance_criteria>
1. `src/Whiteboard.Cli/Services/BatchPipelineOrchestrator.cs` calls `IScriptCompilationOrchestrator` before `IPipelineOrchestrator` for scripted jobs.
2. Scripted job workspaces use the exact deterministic path shape `jobs/{index:000}-{jobId}/compiled-spec.json` and `jobs/{index:000}-{jobId}/compile-report.json` under the summary-output directory.
3. `src/Whiteboard.Cli/Services/BatchPipelineOrchestrator.cs` preserves manifest order and no longer canonicalizes scripted jobs by `jobId`.
4. `tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorTests.cs` asserts compile failures stop before render/export and that ordered scripted jobs execute in declared order.
5. `tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorIntegrationTests.cs` proves at least one scripted batch fixture reaches real export artifacts end to end without manual spec assembly.
6. Verification uses the documented serial CLI build path instead of parallel restore/build.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path 'src/Whiteboard.Cli/Services/BatchPipelineOrchestrator.cs' -Pattern 'IScriptCompilationOrchestrator|IPipelineOrchestrator|compiled-spec.json|compile-report.json' | Out-Null; if (Select-String -Path 'src/Whiteboard.Cli/Services/BatchPipelineOrchestrator.cs' -Pattern 'OrderBy\\(job => job.JobId' -Quiet) { throw 'Scripted batch jobs must preserve manifest order.' }; Select-String -Path 'tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorTests.cs' -Pattern 'compile|render|export|declared order' | Out-Null; Select-String -Path 'tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorIntegrationTests.cs' -Pattern 'script|compiled-spec|export' | Out-Null; $env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'; $env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'; dotnet msbuild 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' /t:Build /restore /m:1 /v:minimal; dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build --no-restore -v minimal --filter \"FullyQualifiedName~BatchPipelineOrchestratorTests|FullyQualifiedName~BatchPipelineOrchestratorIntegrationTests\"</automated>
  </verify>
  <done>Operators can run ordered script batches through the existing compile and pipeline contracts, and Phase 19 scripted jobs no longer require manual spec generation or re-discovery of stage wiring.</done>
</task>

## Verification
```powershell
$env:DOTNET_CLI_HOME = Join-Path (Get-Location) '.dotnet'
$env:DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'
dotnet msbuild 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' /t:Build /restore /m:1 /v:minimal
dotnet test 'tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj' --no-build --no-restore -v minimal --filter "FullyQualifiedName~CliCommandParserTests|FullyQualifiedName~BatchPipelineOrchestratorTests|FullyQualifiedName~BatchPipelineOrchestratorIntegrationTests"
```

## Success Criteria
- `AUT-01` is satisfied for scripted batch jobs through deterministic CLI orchestration rather than manual spec editing.
- Phase 19 scripted jobs preserve declared manifest order and reuse committed compile/report plus render/export contracts without duplicating business semantics.
- The scripted batch flow produces deterministic staged compile artifacts that 19-02 can promote into explicit manifests/status outputs.

## Output
After completion, create `.planning/phases/19-batch-automation-orchestrator/19-01-SUMMARY.md`.
