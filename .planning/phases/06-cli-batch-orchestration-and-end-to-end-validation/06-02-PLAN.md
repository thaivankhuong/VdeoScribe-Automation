---
phase: 6
type: plan
plan: "06-02"
title: Add parser, batch contract, and end-to-end deterministic validation for CLI workflows
wave: 2
depends_on:
  - "06-01"
files_modified:
  - tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj
  - tests/Whiteboard.Cli.Tests/CliCommandParserTests.cs
  - tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorTests.cs
  - tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
  - tests/Whiteboard.Cli.Tests/Fixtures/phase06-cli-batch/primary-manifest.json
  - tests/Whiteboard.Cli.Tests/Fixtures/phase06-cli-batch/equivalent-reordered-manifest.json
  - tests/Whiteboard.Cli.Tests/Fixtures/phase06-cli-batch/job-a-spec.json
  - tests/Whiteboard.Cli.Tests/Fixtures/phase06-cli-batch/job-b-spec.json
autonomous: true
requirements:
  - CLI-01
  - CLI-02
must_haves:
  truths:
    - Parser tests prove the CLI accepts valid single-run and batch inputs, rejects ambiguous or incomplete argument combinations, and preserves deterministic mode selection.
    - Batch orchestration contract tests prove canonical ordering by normalized `jobId`, duplicate `jobId` rejection, aggregate failure handling, persisted `--summary-output` JSON artifact generation for success/failure cases, and batch deterministic key composition without reimplementing pipeline semantics.
    - End-to-end multi-job tests prove repeated runs and equivalent reordered manifests produce identical ordered summaries, identical persisted JSON artifacts, and identical deterministic witnesses.
  artifacts:
    - tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj
    - tests/Whiteboard.Cli.Tests/CliCommandParserTests.cs
    - tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorTests.cs
    - tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs
    - tests/Whiteboard.Cli.Tests/Fixtures/phase06-cli-batch/primary-manifest.json
    - tests/Whiteboard.Cli.Tests/Fixtures/phase06-cli-batch/equivalent-reordered-manifest.json
  key_links:
    - tests/Whiteboard.Cli.Tests/CliCommandParserTests.cs -> src/Whiteboard.Cli/Services/CliCommandParser.cs via explicit run/batch parser, canonical manifest field handling, and batch-only `--summary-output` assertions
    - tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorTests.cs -> src/Whiteboard.Cli/Services/BatchPipelineOrchestrator.cs via normalized `jobId` ordering, aggregation, persisted JSON artifact contents, and failure-policy verification
    - tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs -> src/Whiteboard.Cli/Services/PipelineOrchestrator.cs via real multi-job deterministic parity across repeated and reordered batch inputs
---

<objective>
Lock Phase 06 with focused parser tests, batch orchestration contract coverage, and end-to-end deterministic validation for multi-job CLI workflows, including persisted `--summary-output` JSON summary artifacts suitable for CI automation.
</objective>

## Objective
Lock Phase 06 with focused parser tests, batch orchestration contract coverage, and end-to-end deterministic validation for multi-job CLI workflows, including persisted `--summary-output` JSON summary artifacts suitable for CI automation.

## Constraints
- Execute only after `06-01` establishes the CLI command modes, parser seam, persisted summary artifact behavior, and batch orchestration contracts.
- Keep tests scoped to CLI orchestration behavior; do not reopen engine, renderer, or export semantics.
- Prefer fixture-driven parity checks over ad hoc assertions tied to incidental file-system ordering.
- Keep the verification path serial and stable: build once, then run targeted `dotnet test --no-build` commands.
- Treat mixed-success batches as deterministic aggregate outcomes with stable per-job reporting and failing process status while still writing the JSON artifact.
- Update the CLI test project compile includes when adding new CLI-side test files or linked CLI source files.
- You are not alone in the codebase; extend tests and fixtures without reverting unrelated edits.

<tasks>
  <task id="06-02-01">
    <name>Add parser tests for run and batch command handling</name>
    <action>Create focused `CliCommandParserTests` that cover help text, required arguments, invalid combinations, negative frame-index normalization, manifest-path handling, canonical `jobId/specPath/outputPath/frameIndex` parsing, batch-only required `--summary-output <path>`, and deterministic mode selection.</action>
    <files>tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj; tests/Whiteboard.Cli.Tests/CliCommandParserTests.cs</files>
    <verify>
      <automated>dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~CliCommandParserTests"</automated>
    </verify>
    <done>CLI parser regressions fail with narrow, mode-specific coverage before they can leak into orchestration or end-to-end suites.</done>
  </task>
  <task id="06-02-02">
    <name>Add batch orchestration contract tests for ordering, aggregation, and failure policy</name>
    <action>Create batch-service tests with a fake `IPipelineOrchestrator` to prove canonical ordering by normalized `jobId`, manifest-relative path normalization, duplicate `jobId` rejection, ordered result aggregation, persisted `--summary-output` JSON artifact generation with the locked schema, batch deterministic key composition, and stable mixed-success failure reporting.</action>
    <files>tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj; tests/Whiteboard.Cli.Tests/BatchPipelineOrchestratorTests.cs</files>
    <verify>
      <automated>dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~BatchPipelineOrchestratorTests"</automated>
    </verify>
    <done>Batch orchestration is locked as a deterministic wrapper around repeated single-job runs, with persisted-summary, failure-policy, and ordering rules proven independently from the real pipeline.</done>
  </task>
  <task id="06-02-03">
    <name>Add end-to-end multi-job deterministic validation fixtures</name>
    <action>Extend CLI integration fixtures and `PipelineOrchestratorIntegrationTests` to run real multi-job manifests twice, compare equivalent reordered manifest inputs, and assert identical ordered `jobId` sequences, aggregate counts, export/package witnesses, persisted `--summary-output` JSON artifact contents, and batch deterministic keys.</action>
    <files>tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj; tests/Whiteboard.Cli.Tests/PipelineOrchestratorIntegrationTests.cs; tests/Whiteboard.Cli.Tests/Fixtures/phase06-cli-batch/primary-manifest.json; tests/Whiteboard.Cli.Tests/Fixtures/phase06-cli-batch/equivalent-reordered-manifest.json; tests/Whiteboard.Cli.Tests/Fixtures/phase06-cli-batch/job-a-spec.json; tests/Whiteboard.Cli.Tests/Fixtures/phase06-cli-batch/job-b-spec.json</files>
    <verify>
      <automated>dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~PipelineOrchestratorIntegrationTests"</automated>
    </verify>
    <done>Repeated and reordered multi-job runs prove end-to-end CLI determinism at both job and persisted batch-summary levels.</done>
  </task>
</tasks>

## Verification
Automated commands:
```powershell
dotnet build "whiteboard-engine.sln" --no-restore -v minimal /m:1
dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~CliCommandParserTests"
dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~BatchPipelineOrchestratorTests"
dotnet test "tests/Whiteboard.Cli.Tests/Whiteboard.Cli.Tests.csproj" --no-build -v minimal --filter "FullyQualifiedName~PipelineOrchestratorIntegrationTests"
```

Manual checks:
- Re-run the same manifest twice and confirm the human-readable summary stays aligned with the canonical aggregate fields and the persisted `--summary-output` JSON artifact contents.
- Compare two logically equivalent manifests with different source ordering and confirm the reported `jobId` order follows the documented canonical key while the persisted JSON artifact remains identical.
