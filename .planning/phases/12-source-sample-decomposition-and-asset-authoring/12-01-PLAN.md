---
phase: 12-source-sample-decomposition-and-asset-authoring
plan: 01
title: Define the authored parity asset set and witness-scene object decomposition
wave: 1
depends_on: []
requirements: [PAR-01, AST-01]
files_modified:
  - artifacts/source-parity-demo/build-engine-assets.ps1
  - artifacts/source-parity-demo/engine-assets/*.svg
  - artifacts/source-parity-demo/assets/hand.svg
  - artifacts/source-parity-demo/check/*
autonomous: true
must_haves:
  truths:
    - The active parity witness scene is backed by authored visual assets rather than whole-frame or segmented source-image crops.
    - The witness scene has a locked 6-object decomposition that matches the reviewable scene structure needed for later motion and fidelity phases.
    - Hand content remains an independent manifest-backed asset instead of being baked into authored scene objects.
  artifacts:
    - artifacts/source-parity-demo/build-engine-assets.ps1
    - artifacts/source-parity-demo/engine-assets/*.svg
    - artifacts/source-parity-demo/assets/hand.svg
  key_links:
    - artifacts/source-parity-demo/build-engine-assets.ps1 -> artifacts/source-parity-demo/engine-assets/*.svg via repeatable generation of authored witness assets
    - artifacts/source-parity-demo/engine-assets/*.svg -> artifacts/source-parity-demo/project-engine.json via object-level asset references used by the witness scene
    - artifacts/source-parity-demo/assets/hand.svg -> assets.handAssets in the witness spec via independent hand guidance plumbing
---

## Objective
Lock the authored asset inventory and the 6-object witness-scene decomposition so Phase 12 starts from reusable VideoScribe-like content instead of crop-derived shortcuts.

## Constraints
- Final witness content must remain authored and engine-first.
- Source frames may only be used as reference/tracing input during authoring.
- Do not broaden this plan into motion-timing or text-fidelity polish reserved for later phases.

## Tasks
<task>
  <name>Normalize the witness asset inventory</name>
  <action>Review the current parity artifact set, keep the authored SVG and hand assets that belong to the engine-first path, and tighten naming or generation flow so the active asset set is clearly separated from shortcut crop branches.</action>
  <done>The repo contains a clear authored parity asset set for the witness scene and it is obvious which files belong to the active path.</done>
</task>

<task>
  <name>Lock the 6-object decomposition against the witness scene</name>
  <action>Make the asset set explicitly map to the reviewable object breakdown of left illustration, arrow, title, clock group, body, and footer while keeping text grouped into authored title/body/footer blocks and the clock content as one group for this phase.</action>
  <done>Phase 12 has a stable object decomposition that downstream plans can wire without reopening the scene breakdown.</done>
</task>

<task>
  <name>Preserve hand separation for later sequencing work</name>
  <action>Keep the hand asset independent from authored scene content and verify the parity witness assets do not bake hand imagery into the six object assets.</action>
  <done>Later hand-follow work can use the authored witness assets without undoing Phase 12 asset boundaries.</done>
</task>

## Verification
```powershell
& 'artifacts/source-parity-demo/build-engine-assets.ps1'
Get-ChildItem 'artifacts/source-parity-demo/engine-assets'
```
