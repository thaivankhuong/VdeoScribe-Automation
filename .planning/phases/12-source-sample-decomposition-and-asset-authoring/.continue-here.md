---
phase: 12-source-sample-decomposition-and-asset-authoring
task: 0
total_tasks: 3
status: planned
last_updated: 2026-03-21T16:14:54.386Z
---

<current_state>
Phase 12 has been discussed and planned. The milestone direction is now explicit: build a reusable VideoScribe-like authored asset workflow first, and use the current reference sample only as a witness scene.

The immediate repo state for GSD is:
- 12-CONTEXT.md created and committed
- 12-01/12-02/12-03 plan files created and committed
- Phase 12 is ready for execution

Do not resume from crop-based parity thinking. The active path is authored assets, spec-driven manifests, and deterministic witness rendering.
</current_state>

<completed_work>
- Phase 12 context captured in `12-CONTEXT.md`.
- Locked decisions:
  - final content must come from authored SVG/text assets
  - source video may be used only as reference/tracing input
  - hand remains a separate manifest-backed asset
  - witness scene stays at 6 objects for this phase
  - the broader goal is a reusable VideoScribe-like asset library, not a one-off sample clone
- Phase 12 plans created:
  - Task 1 / Plan 12-01: define authored parity asset set and object decomposition
  - Task 2 / Plan 12-02: wire authored witness spec and manifests through the pipeline
  - Task 3 / Plan 12-03: validate deterministic authored output and demote crop-based main-path shortcuts
- Plan review was performed and one gap in 12-03 verification was fixed by requiring an actual CLI render witness run.
</completed_work>

<remaining_work>
- Task 1: Execute `12-01-PLAN.md`.
- Task 2: Execute `12-02-PLAN.md` after 12-01 is complete.
- Task 3: Execute `12-03-PLAN.md` after 12-01 and 12-02 are complete.
- After Phase 12 execution, move to Phase 13 for motion and hand sequencing parity.
</remaining_work>

<decisions_made>
- Use engine-first authored assets as the only valid main path because the user rejected shortcut rendering from source crops.
- Treat the current sample as a witness scene because the real target is a reusable VideoScribe-like library of whiteboard assets and transitions.
- Keep Phase 12 scoped to asset authoring and spec/manifest wiring; motion polish, hand-follow timing, and fidelity tuning stay in later phases.
- Keep the witness scene decomposed into 6 reviewable objects because that is enough structure for this phase without over-engineering the asset system.
</decisions_made>

<blockers>
- The repo has a very dirty working tree with many unrelated source and planning changes already present. Do not revert them. Scope future commits narrowly.
- Parallel `dotnet test` runs remain unreliable due to obj-lock issues; prefer serial verification commands.
- `apply_patch` hit Windows sandbox refresh failures in this session; PowerShell file writes were the reliable fallback.
</blockers>

<context>
The correct mental model when resuming is:
- We are not chasing screenshot parity by shortcuts.
- We are building the first authored asset-library slice of a VideoScribe-like engine.
- Sample parity is only the proof harness for the new asset workflow.
- Execution should keep module boundaries intact: Core validates, CLI resolves, Renderer consumes, Export packages.

The next work should feel mechanical rather than exploratory: execute the three existing plans in order and keep evidence deterministic.
</context>

<next_action>
Start with: open `12-01-PLAN.md`, execute the authored asset inventory cleanup and 6-object decomposition work, then verify with `artifacts/source-parity-demo/build-engine-assets.ps1` and `Get-ChildItem 'artifacts/source-parity-demo/engine-assets'`.
</next_action>
