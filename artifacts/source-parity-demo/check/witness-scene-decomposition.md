# Authored Witness Scene Decomposition

Active main path: `project-engine.json`

This witness scene is intentionally locked to six authored scene objects:

1. object-left -> engine-assets/left-illustration.svg (Left Illustration)
   Checklist board and characters remain one authored illustration object for Phase 12.
2. object-arrow -> engine-assets/arrow-main.svg (Arrow)
   Connector arrow stays separate so later motion timing can target it directly.
3. object-title -> engine-assets/title.svg (Title)
   Headline text is authored as its own SVG block instead of a source crop.
4. object-clock-group -> engine-assets/clock-group.svg (Clock Group)
   Clock illustration plus nearby text stay grouped as one witness object for this phase.
5. object-body -> engine-assets/body.svg (Body)
   Body copy is its own authored object and remains separate from the title/footer blocks.
6. object-footer -> engine-assets/footer.svg (Footer)
   Footer text stays separate from the witness background and crop-based references.

Hand asset: `assets/hand.svg` remains separate from all six scene objects.
Legacy crop specs (`project.json`, `project-image-hand.json`) remain comparison-only inputs.
Generated by `build-engine-assets.ps1` to make the authored parity inventory explicit for Phase 12.